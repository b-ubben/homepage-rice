import styled from 'styled-components';

const Wrapper = styled.main`
  display: flex;
  flex-direction: column;
  margin: 0 auto;
  max-width: calc(760px + 0.25rem);
  min-height: 100vh;
  width: 100%;
`;

export default Wrapper;
